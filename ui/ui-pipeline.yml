workflow:
  rules:
    - if: $CI_MERGE_REQUEST_ID

include:
  - ci/child-pipeline.yml

tests:
  stage: test
  extends: .cache_settings
  script:
    - echo "Disabled!"
  cache:
    policy: pull
  artifacts: {}
